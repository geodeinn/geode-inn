{
  "name": "geode-inn-arcade-mobile",
  "version": "1.0.0",
  "description": "The Geode Inn Arcade — mobile edition",
  "scripts": {
    "sync:ios": "npx cap sync ios",
    "sync:android": "npx cap sync android",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "build:web": "cp ../../web_games/geode_inn.html www/arcade.html",
    "prepare": "npm run build:web && npx cap sync"
  },
  "devDependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}
